# Binaries
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
my-personal-cloud
my-personal-cloud-server

# Test binary
*.test
*.out

# Go workspace
go.work
go.work.sum

# Dependency directories (Node)
web/node_modules/

# Vite build output — exclude generated assets but keep the placeholder so
# `go build` (which uses //go:embed all:dist) succeeds on a fresh clone.
web/dist/*
!web/dist/.gitkeep

# Local data and uploads
/data/
/storage/
*.db
*.db-journal

# Environment / secrets
.env
.env.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
